export class User {
    //Construtor
    constructor(user, password, createDate, createTime) {
        this.user = user;
        this.password = password;
        this.createDate = createDate;
        this.createTime = createTime;
    }
}
